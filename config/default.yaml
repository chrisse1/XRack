application:
  name: "XRack"
  version: "0.9.7"
  # "de" oder "en" - wird von install.sh in config/local.yaml
  # abgefragt und überschrieben.
  language: "de"

server:
  host: "0.0.0.0"
  port: 8080

recording:
  directory: "./recordings"

music:
  directory: "./music"

logging:
  level: "INFO"
